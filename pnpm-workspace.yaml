packages:
  - packages/*
  - bench/*
allowBuilds:
  # Neither needs its install script: Jest ships prebuilt resolver bindings and never watches here.
  '@parcel/watcher': false
  unrs-resolver: false
